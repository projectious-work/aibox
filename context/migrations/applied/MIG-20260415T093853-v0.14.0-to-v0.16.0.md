---
apiVersion: processkit.projectious.work/v1
kind: Migration
metadata:
  id: MIG-20260415T093853
  created: 2026-04-15T09:38:53Z
spec:
  source: processkit
  source_url: "https://github.com/projectious-work/processkit.git"
  from_version: v0.14.0
  to_version: v0.16.0
  state: applied
  applied_at: 2026-04-15T10:30:00Z
  generated_by: "manual reconstruction (BACK-20260415_0938)"
  generated_at: 2026-04-15T09:38:53Z
  summary: "2 version spans (v0.14.0→v0.15.0, v0.15.0→v0.16.0); 8+ changed upstream files, multiple new skills/scripts, AGENTS.md conflicts, canonical team schema additions"
  affected_groups:
    - AGENTS
    - skill-gate
    - team-creator
    - morning-briefing
    - roles
    - actors
---

# Migration MIG-20260415T093853

Manually reconstructed migration from `v0.14.0` to `v0.16.0`
(source: `https://github.com/projectious-work/processkit.git`).

This migration was originally generated incorrectly by `aibox sync` as
a no-op `v0.16.0 → v0.16.0` transition, masking two full version spans of
upstream changes. See `BACK-20260415_0938-SilentEagle-sync-wrong-version`
for the root cause and fix.

---

## From v0.14.0 — Enforcement Rails 1–4

These changes are already present in the v0.14.0 baseline snapshot at
`context/templates/processkit/v0.14.0/` (restored by this recovery).

### AGENTS.md primacy header

The compliance-contract block was added at the top of the canonical
`AGENTS.md` template:

```
<!-- pk-compliance-contract v1 BEGIN -->
<!-- pk-compliance v1 -->

## processkit Compliance Contract
...
<!-- pk-compliance-contract v1 END -->
```

This block enforces processkit operational contracts (route_task,
skill-finder consultation, entity creation in same turn, MCP writes,
index reads, event logging, decision recording, template immutability,
harness MCP config immutability) at the top of every agent's AGENTS.md.

### skill-gate/scripts/emit_compliance_contract.py

New script added to `skill-gate/scripts/`. Generates the compliance
contract block for injection into AGENTS.md during sync. Part of
Rail 1 (visibility of compliance obligations at session start).

### skill-gate/scripts/check_route_task_called.py

New script added to `skill-gate/scripts/`. Validates that `route_task`
was called before mutating MCP tool invocations. Part of Rail 2
(call-order enforcement).

### skill-gate/mcp — acknowledge_contract() tool

New MCP tool `acknowledge_contract()` added to the `skill-gate` MCP
server. Agents call this to record contract acknowledgement at session
start. Part of Rail 3 (explicit acknowledgement).

### 1%-rule sentence in 8 mutating MCP tool descriptions

The 1%-rule sentence ("If there is even a 1% chance a processkit skill
applies to the current task, consult `skill-finder` before acting") was
added to the description of 8 mutating MCP tools across
`artifact-management`, `decision-record`, `discussion-management`,
`event-log`, `skill-finder`, `skill-gate`, `task-router`, and
`workitem-management`. Part of Rail 4 (inline reminder in tool descriptions).

---

## v0.15.0 Changes

### New skill: team-creator

A new composite skill `team-creator` was added. It composes:
- `model-recommender` (model selection by task profile)
- `role-management` (role CRUD)
- `actor-profile` (actor CRUD and template management)
- `binding-management` (role-actor binding lifecycle)

Weights: `model-recommender` 0.60 / `role-management` 0.20 /
`actor-profile` 0.10 / `binding-management` 0.10.

New commands provided:
- `team-create` — scaffold a team from a role-set definition
- `team-review` — audit current team against workload and model-mix targets
- `team-rebalance` — suggest and apply model-tier adjustments

### AGENTS.md session-orientation block

A session-orientation block was added to the canonical AGENTS.md
template (under the AI agents section). It guides agents on how to
orient themselves at session start: read the briefing, check pending
migrations, load INDEX.md before slurping directories.

### morning-briefing 1.0.0 → 1.1.0

The `morning-briefing` skill was updated from 1.0.0 to 1.1.0. The
updated version reads `context/migrations/pending/` and surfaces any
pending migrations in the morning briefing output, ensuring agents are
aware of outstanding migration tasks.

---

## v0.16.0 Changes

### Canonical team schema additions

The canonical processkit team schema was updated to add the following
fields to the `Role` and `Actor` kinds:

**Role additions:**
- `spec.primary_contact: bool` — marks the role that speaks to the
  project owner. One role per team should set this to `true`.
- `spec.clone_cap: int` — maximum number of simultaneous clones for this
  role (canonical replacement for project-local `x_aibox.default_clone_cap`).
- `spec.cap_escalation: string` — escalation path when clone_cap is
  reached (canonical replacement for project-local `x_aibox.escalate_cap_to`).

**Actor additions:**
- `spec.is_template: bool` — marks this actor as a role template (never
  reused; clones get fresh IDs). Canonical replacement for
  `x_aibox.is_template`.
- `spec.templated_from: string | null` — ID of the template actor this
  clone was created from. Canonical replacement for `x_aibox.clone_of`.

### Back-fill migrations

Two back-fill migrations ship in this version upstream:
1. `default_clone_cap` → `spec.clone_cap`
2. `escalate_cap_to` → `spec.cap_escalation`
3. `clone_of` → `spec.templated_from`
4. `is_template` moved from `x_aibox.is_template` → `spec.is_template`

---

## Local Resolution Applied

### AGENTS.md conflict — resolved

**Kept local:**
- 8-role team section with team table, model-mix targets, clone notes,
  directory references, decision record cross-link
- Team/budget narrative (5% Opus / 85% Sonnet / 10% Haiku)
- Project-local GitHub block (repo, GHCR, docs, processkit upstream)
- All aibox-specific sections (project structure, critical devcontainer
  note, aibox⇄processkit boundary, anti-patterns, design principles,
  provider independence, operational gotchas, runtime artifacts)

**Ported from v0.16.0 template:**
- Compliance contract primacy block (top of file, Rails 1–4)
- Session-orientation guidance (under AI agents section)
- processkit version updated from v0.13.0 → v0.16.0
- Footer date updated to 2026-04-15

### Team entity schema migration — applied

All 8 Role files and 8 Agent Actor files updated:
- `spec.clone_cap` (from `x_aibox.default_clone_cap`)
- `spec.cap_escalation` (from `x_aibox.escalate_cap_to`)
- `spec.is_template` (from `x_aibox.is_template`)
- `spec.templated_from` (from `x_aibox.clone_of`)
- `spec.primary_contact: true` added to project-manager role
- Old `x_aibox.schema_note` fields removed

### v0.14.0 baseline template

Step 1 (download v0.14.0 tarball) was skipped — network access (curl)
was unavailable in the execution environment. The `context/templates/processkit/v0.14.0/`
directory was not created. This should be completed manually or by
re-running `aibox sync` once the sync bug is fixed.

---

## Cross-link

`BACK-20260415_0938-SilentEagle-sync-wrong-version`
