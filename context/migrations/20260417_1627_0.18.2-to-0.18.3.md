# Migration: v0.18.2 → v0.18.3

> **SAFETY: Do not execute any actions automatically.**
> **Discuss each item with the project owner before proceeding.**
> **Do not modify aibox.toml without explicit user confirmation.**
> **`aibox` commands run on the HOST, outside the container — you cannot run them.**

**Generated:** 2026-04-17
**Status:** completed
**aibox CLI:** v0.18.2 → v0.18.3
**processkit:** v0.17.0

## Summary

aibox has been updated from v0.18.2 to v0.18.3. Review each section below
and discuss action items with the project owner.

## Breaking Changes

Review the release notes for each version in this upgrade:
- https://github.com/projectious-work/aibox/releases/tag/v0.18.3

## Action Items

### Host actions (owner runs these outside the container)

- [ ] Owner: verify `aibox sync` was run for v0.18.3 — check `aibox.lock` at
      `/workspace/aibox.lock`: `[aibox].cli_version` should equal `0.18.3` and
      `synced_at` should be a recent timestamp
- [ ] Owner: if sync has NOT been run, run `aibox sync` on the host, then `aibox build`
- [ ] Owner: if the container was not rebuilt after sync, run `aibox build` then `aibox start`

### Agent verification (you can do these now)

- [x] Read `/workspace/aibox.lock` — confirm `[aibox].cli_version = "0.18.3"`
- [x] Read `/workspace/aibox.lock` — confirm `[processkit].version` matches `/workspace/aibox.toml [processkit].version` -- both v0.17.0 (toml "latest" resolved)
- [x] Verify `/workspace/AGENTS.md` exists and is non-empty -- 445 lines
- [x] Verify `/workspace/context/skills/` directory exists
- [x] Verify `/workspace/context/skills/skill-finder/` exists (core skill) -- at context/skills/processkit/skill-finder
- [x] Verify `/workspace/context/processes/` directory exists
- [x] Verify `/workspace/context/schemas/` directory exists
- [x] Verify `/workspace/context/templates/processkit/v0.17.0/` snapshot directory exists
- [x] Check `/workspace/context/migrations/` for other unreviewed CLI migration documents -- 0.18.1→0.18.2 is the prior sequential, now also marked completed
- [x] Check `/workspace/context/migrations/pending/` for processkit content migration files -- MIG-RUNTIME-20260417T142743 handled separately and moved to applied/
- [x] Mark this migration as completed

## processkit State

processkit is tracking `version = "latest"` — installed: `v0.17.0` (upgraded from `v0.17.0` — aibox.toml now targets `vlatest`) (source: `https://github.com/projectious-work/processkit.git`).

**Upgrade policy for `version = "latest"`:**
`aibox sync` resolves `latest` at run time using a semver-aware policy:
- **Patch / minor upgrades** (same major): applied automatically.
- **Major upgrades**: blocked — a warning is shown and sync stays on the
latest release within the current major. To cross a major boundary, pin
an explicit version in `aibox.toml` (e.g. `version = "v2.0.0"`).

**Check for pending processkit content migrations:**
Look in `/workspace/context/migrations/pending/` — `aibox sync` deposits
content migration documents there during the 3-way diff. Do NOT skip this step.

If files exist, do NOT handle them manually. Instead:

1. Use `skill-finder` to locate the processkit migration management skill
(search for "migration" or "content update").
2. Invoke that skill — it knows the correct workflow, state machine, and document
format for reviewing and applying processkit content migrations.
3. Work through each pending migration with the owner before marking it applied.

processkit owns the migration format and workflow; defer entirely to its skill.

## AGENTS.md Review

After the upgrade, verify `AGENTS.md` is current:
- [x] processkit version reference matches `aibox.lock` (`v0.17.0`) -- AGENTS.md line 141 + 445 updated to v0.17.0
- [x] Configured AI harnesses/providers match the `[ai]` section in `aibox.toml` -- claude + codex (aibox.toml) match AGENTS.md
- [x] Build / test / lint commands are still accurate
- [x] Project-specific notes and operational gotchas are up to date

## Verification Summary

After all host actions are confirmed and agent verifications pass, mark Status as "completed".

## Rollback

To revert this migration (owner runs on host):
```
git checkout HEAD~1 -- aibox.lock context/ .devcontainer/
aibox sync
```

## Known Issues

Check https://github.com/projectious-work/aibox/issues for known issues with v0.18.3.
