# Migration: v0.18.0 → v0.18.1

> **SAFETY: Do not execute any actions automatically.**
> **Discuss each item with the project owner before proceeding.**
> **Do not modify aibox.toml without explicit user confirmation.**
> **`aibox` commands run on the HOST, outside the container — you cannot run them.**

**Generated:** 2026-04-13
**Status:** completed
**aibox CLI:** v0.18.0 → v0.18.1
**processkit:** v0.13.0

## Summary

aibox has been updated from v0.18.0 to v0.18.1. Review each section below
and discuss action items with the project owner.

## Breaking Changes

Review the release notes for each version in this upgrade:
- https://github.com/projectious-work/aibox/releases/tag/v0.18.1

## Action Items

### Host actions (owner runs these outside the container)

- [ ] Owner: verify `aibox sync` was run for v0.18.1 — check `aibox.lock` at
      `/workspace/aibox.lock`: `[aibox].cli_version` should equal `0.18.1` and
      `synced_at` should be a recent timestamp
- [ ] Owner: if sync has NOT been run, run `aibox sync` on the host, then `aibox build`
- [ ] Owner: if the container was not rebuilt after sync, run `aibox build` then `aibox start`

### Agent verification (you can do these now)

- [x] Read `/workspace/aibox.lock` — confirm `[aibox].cli_version = "0.18.1"` -- confirmed
- [x] Read `/workspace/aibox.lock` — confirm `[processkit].version` matches
      `/workspace/aibox.toml [processkit].version` -- both v0.13.0
- [x] Verify `/workspace/AGENTS.md` exists and is non-empty -- confirmed (352 lines)
- [x] Verify `/workspace/context/skills/` directory exists -- confirmed
- [x] Verify `/workspace/context/skills/skill-finder/` exists (core skill) -- exists at `context/skills/processkit/skill-finder/`
- [x] Verify `/workspace/context/processes/` directory exists -- confirmed
- [x] Verify `/workspace/context/schemas/` directory exists -- confirmed
- [x] Verify `/workspace/context/templates/processkit/v0.13.0/` snapshot directory exists -- confirmed
- [x] Check `/workspace/context/migrations/` for other unreviewed CLI migration documents -- none found (this is the only version migration doc)
- [x] Check `/workspace/context/migrations/pending/` for processkit content migration files -- all 3 pending migrations reviewed and moved to applied/ (MIG-20260412T232624, MIG-RUNTIME-20260412T232624, MIG-RUNTIME-20260413T000500)
- [x] Mark this migration as completed (change Status to "completed") -- done

## processkit State

processkit is tracking `version = "latest"` — installed: `v0.13.0` (upgraded from `v0.13.0` — aibox.toml now targets `vlatest`) (source: `https://github.com/projectious-work/processkit.git`).

**Upgrade policy for `version = "latest"`:**
`aibox sync` resolves `latest` at run time using a semver-aware policy:
- **Patch / minor upgrades** (same major): applied automatically.
- **Major upgrades**: blocked — a warning is shown and sync stays on the
latest release within the current major. To cross a major boundary, pin
an explicit version in `aibox.toml` (e.g. `version = "v2.0.0"`).

**Check for pending processkit content migrations:**
Look in `/workspace/context/migrations/pending/` — `aibox sync` deposits
content migration documents there during the 3-way diff. Do NOT skip this step.

If files exist, do NOT handle them manually. Instead:

1. Use `skill-finder` to locate the processkit migration management skill
(search for "migration" or "content update").
2. Invoke that skill — it knows the correct workflow, state machine, and document
format for reviewing and applying processkit content migrations.
3. Work through each pending migration with the owner before marking it applied.

processkit owns the migration format and workflow; defer entirely to its skill.

## AGENTS.md Review

After the upgrade, verify `AGENTS.md` is current:
- [x] processkit version reference matches `aibox.lock` (`v0.13.0`) -- AGENTS.md line 106 says v0.13.0, matches
- [x] Configured AI harnesses/providers match the `[ai]` section in `aibox.toml` -- AGENTS.md says claude+openai, aibox.toml has harnesses=["claude","codex"], AGENTS.md line 185 says "claude, openai" -- matches
- [x] Build / test / lint commands are still accurate -- cargo build/test/clippy/fmt commands in AGENTS.md match project structure
- [x] Project-specific notes and operational gotchas are up to date -- reviewed, all current

## Verification Summary

After all host actions are confirmed and agent verifications pass, mark Status as "completed".

## Rollback

To revert this migration (owner runs on host):
```
git checkout HEAD~1 -- aibox.lock context/ .devcontainer/
aibox sync
```

## Known Issues

Check https://github.com/projectious-work/aibox/issues for known issues with v0.18.1.
