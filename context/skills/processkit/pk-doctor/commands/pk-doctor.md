---
argument-hint: "[--category=...] [--fix=... | --fix-all] [--since=<ref>] [--yes]"
allowed-tools: []
---

Aggregator health-check for the processkit repository. Runs detect-only checks by
default; use `--fix` or `--fix-all` to enable fixes. Use when checking repository
health, after upgrades, or before releases.
