# PulseAudio over TCP — used for Claude Code voice in containers
# PULSE_SERVER env var must be set (default: tcp:host.docker.internal:4714)
pcm.default pulse
ctl.default pulse

pcm.pulse {
    type pulse
}

ctl.pulse {
    type pulse
}
